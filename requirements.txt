Django==3.2.5
Flask==2.0.1

# Django dependencies
asgiref==3.4.1
pytz==2021.1
sqlparse==0.4.1

# Flask dependencies
Werkzeug==2.0.1
Jinja2==3.0.1
itsdangerous==2.0.1
click==8.0.1

# Database drivers
psycopg2-binary==2.9.1  # Switched to binary to avoid pg_config issue
mysqlclient==2.0.3  # Note: This may also fail on Netlify unless MySQL dev headers are present

# Other common dependencies
gunicorn==20.1.0
requests==2.25.1
